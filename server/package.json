{
  "name": "marketlens-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MarketLens data backend: holds provider credentials server-side and exposes the JSON contracts the frontend consumes.",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env index.js",
    "dev": "node --watch --env-file-if-exists=.env index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "viem": "^2.56.5"
  }
}
